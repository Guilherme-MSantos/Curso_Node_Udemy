const os = require("os");

console.log(os.cpus()); // quantas cpus tem o servidor
console.log(os.freemem()); // quanta memoria livre a maquina tem
console.log(os.homedir()); // qual o dir da home
console.log(os.type()); // tipo de sistema operacional da maquina
